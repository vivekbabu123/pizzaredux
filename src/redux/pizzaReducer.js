import { BUY_PIZZA } from "./pizzaActionTypes";

let initialState = {
    count :35
};


export const  pizzaReducer = (state, action) =>{
    switch(action.type){
        case BUY_PIZZA:
            return{
                
            }
    }
};